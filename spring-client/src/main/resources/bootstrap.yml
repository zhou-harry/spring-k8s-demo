spring:
  application:
    name: spring-k8s-client
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
server:
  port: 8080

management:
  server:
    port: 8081
  endpoints:
    web:
      exposure:
        include: "*"
ribbon:
  eureka:
    enabled: false