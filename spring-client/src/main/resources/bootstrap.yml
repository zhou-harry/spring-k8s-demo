spring:
  application:
    name: spring-k8s-client

server:
  port: 8080

management:
  server:
    port: 8081
  endpoints:
    web:
      exposure:
        include: "*"
ribbon:
  eureka:
    enabled: false
#开启 Hystrix
feign:
  hystrix:
    enabled: true